image: node:24

cache:
  key: "$CI_COMMIT_REF_NAME"
  paths:
   - node_modules
   - .npm

before_script:
 - npm ci --cache .npm --prefer-offline

lint:
  script:
   - npm run lint

test:
  script:
   - npm test
